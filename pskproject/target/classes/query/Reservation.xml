<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pension.client.reservation.dao.ReservationDao">
	<select id="reservationList" parameterType="reservation" resultType="reservation">
		SELECT
		res_no, r_no, m_no, res_date, res_night, res_people, res_price, res_bbq,
		res_update, res_indate, res_outdate
		FROM
		reservation
		ORDER BY res_no desc
	</select>
	
	<!-- 예약 등록 -->
	<insert id="reservationInsert" parameterType="reservation">
		<selectKey keyProperty="res_no" resultType="int" order="BEFORE">
			select res_no from dual
		</selectKey>
		INSERT into reservation(res_no, r_no, m_no, res_date, res_night, res_people, res_price, 
		res_update, res_indate, res_outdate, res_bbq)
		values(
		#{res_no}, #{r_no}, #{m_no}, #{res_date}, #{res_nigt}, #{res_people}, #{res_price}, 
		#{res_update}, #{res_indate}, #{res_outdate}, #{res_bbq})
		)
	</insert>
</mapper>